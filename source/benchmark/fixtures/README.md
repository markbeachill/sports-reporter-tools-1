# Fixtures

Store stable testing materials here, such as prompt versions, scoring variants or reference notes used across benchmark runs.

The main prompt tools are stored at the top level of the archive. Only copy them here if you need a frozen benchmark-specific version.
